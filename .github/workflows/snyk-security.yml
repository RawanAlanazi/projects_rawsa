name: Snyk

on:
 push:
 pull_request:

jobs:
  snyk:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: snyk/actions/setup@master

      - run: snyk test
        env:
          SNYK_TOKEN: ${{40795790-a71e-46b4-98a7-25afc12e6073 }}
